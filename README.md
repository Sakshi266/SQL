## 📊 SQL 

This project contains SQL queries used for data cleaning, transformation, and analysis on a structured dataset. The goal is to demonstrate strong SQL skills using operations like GROUP BY, UPDATE, CASE, filtering, aggregation, and date formatting.
